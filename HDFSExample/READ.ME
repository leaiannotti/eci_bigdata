I have created two java classes with the HDFS APIs.

The first one (HDFSWriter) takes a local file and writes it to an output folder of the hadoop file system.

The second one (HDFSReader) read the file previously created and write its contents back to the local file system.